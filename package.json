{
  "name": "yapa-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Technical demo: card recommender based on merchant MCC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "demo": "npm run demo --workspace=@yapa/engine"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
